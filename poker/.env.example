# Frontend environment variables
# Base for backend API calls.
# - Dev: leave empty to use Vite proxy to http://localhost:4000
# - Prod: set to /poker so requests go to /poker/api/* behind Nginx
VITE_BACKEND_URL=

# Optional public client API key used by the backend proxy (/api/client/*)
# Set only if your backend is configured to require a client key
VITE_CLIENT_API_SECRET=

# Other vars used by the app
VITE_MULTISYNQ_API_KEY=

# Privy App ID for this app (from your Privy dashboard)
VITE_PRIVY_APP_ID=

# Note: Monad Games Cross App ID is configured in code via loginMethodsAndOrder
